# Serves the Vite SPA build; unknown paths fall back to index.html so
# client-side routing works on refresh/direct navigation.
server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
